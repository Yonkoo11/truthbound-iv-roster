# Python 3.10+ required (or any version with `from __future__ import annotations`)
# Core CLI
rich==14.3.3
tabulate==0.10.0

# Scout sources
requests==2.32.5
beautifulsoup4==4.14.3

# SQLite is stdlib — no extra dep needed
# python-dotenv not required — .env loading is done manually in notify.py
